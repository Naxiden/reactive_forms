import { NoNotAllowedSymbolsDirective } from './no-not-allowed-symbols.directive';

describe('NoNotAllowedSymbolsDirective', () => {
  it('should create an instance', () => {
    const directive = new NoNotAllowedSymbolsDirective();
    expect(directive).toBeTruthy();
  });
});
